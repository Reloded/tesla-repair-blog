---
layout: base.njk
title: Tesla Body & Exterior Repair Guides
description: DIY guides for Tesla paint chip repair, bumper removal, panel gap adjustment, window tint, glass roof fixes, and body work. All Tesla models covered.
permalink: /guides/exterior-body/
---

<div class="section-header" style="margin-bottom: 2rem;">
  <h1>🚗 Tesla Body & Exterior Guides</h1>
  <p>Paint chips, bumper removal, panel gaps, window tinting, glass roof issues, and more. Keep your Tesla looking factory-fresh with these hands-on body repair guides.</p>
  <p><a href="/guides/">← Back to all guides</a></p>
</div>

<div class="posts-grid">
  {% for post in collections.posts | reverse %}
    {% set cat = post.data.category | lower %}
    {% if cat == "exterior" or cat == "body" or cat == "body & exterior" or cat == "body & glass" or cat == "body & trim" or cat == "exterior & body" %}
    <article class="post-card">
      <div class="post-card-visual cat-exterior">
        <span class="cat-icon">🚗</span>
        <span class="difficulty-tag">{{ post.data.difficulty or "Easy" }}</span>
        <span class="cat-label">{{ post.data.category }}</span>
      </div>
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <p class="post-excerpt">{{ post.data.description }}</p>
    </article>
    {% endif %}
  {% endfor %}
</div>
