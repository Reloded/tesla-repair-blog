---
phase: 02-analytics-seo
type: execute
---

<objective>
Set up analytics tracking and optimize for search engines.

Purpose: Track visitors and improve Google discoverability.
Output: Google Analytics configured, Open Graph tags enhanced, schema markup added.
</objective>

<context>
@.planning/PROJECT.md
@src/_includes/base.njk
@src/_includes/post.njk
@src/_data/metadata.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Get Google Analytics Measurement ID</action>
  <instructions>
    1. Go to https://analytics.google.com
    2. Create account or sign in
    3. Create a new property for your blog
    4. Get your Measurement ID (starts with G-)
  </instructions>
  <verification>User provides G-XXXXXXXX ID</verification>
  <resume-signal>Paste your Google Analytics Measurement ID (or "skip" to continue without)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add Open Graph image meta tags</name>
  <files>src/_includes/base.njk</files>
  <action>Add og:image meta tag pointing to a default social sharing image. Create placeholder reference for now.</action>
  <verify>Build succeeds, og:image tag present in HTML</verify>
  <done>Open Graph image meta tag added to base template</done>
</task>

<task type="auto">
  <name>Task 2: Add article schema markup</name>
  <files>src/_includes/post.njk</files>
  <action>Add JSON-LD schema markup for Article type to post template. Include headline, datePublished, author, description.</action>
  <verify>Build succeeds, schema visible in page source</verify>
  <done>Article schema markup in post pages</done>
</task>

<task type="auto">
  <name>Task 3: Enhance meta descriptions</name>
  <files>src/_includes/base.njk</files>
  <action>Ensure meta description is properly truncated and formatted for search results (max 160 chars).</action>
  <verify>Build succeeds</verify>
  <done>Meta descriptions optimized</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Open Graph tags present in HTML head
- [ ] Schema markup present in article pages
</verification>

<success_criteria>
- Google Analytics ready (or skipped)
- Open Graph image tag added
- Article schema markup on posts
- SEO meta tags optimized
</success_criteria>

<output>
Create `.planning/phases/02-analytics-seo/02-01-SUMMARY.md` after completion.
</output>
